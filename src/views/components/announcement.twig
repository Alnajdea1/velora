{% if theme.settings.get('announcement_enabled', true) %}<div class="v-announce v-dark" style="padding:9px 20px;text-align:center;font-size:12.5px">
  <span style="color:var(--lilac)">{{ trans('announcement.prefix') }}</span>
  {{ trans('announcement.text') }}
  <b style="color:var(--coral)">{{ theme.settings.get('announcement_code', 'VELORA10') }}</b>
</div>
{% endif %}
