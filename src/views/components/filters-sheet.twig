<div class="v-sheet" data-overlay="filters" hidden>
  <div class="v-sheet__panel">
    <span class="v-sheet__grab" aria-hidden="true"></span>
    <salla-filters></salla-filters>
    <div style="display:flex;gap:10px;margin-top:18px">
      <button type="button" class="v-btn v-btn--ghost" data-clear-filters style="flex:1;justify-content:center">{{ trans('collection.clear') }}</button>
      <button type="button" class="v-btn v-btn--dark" data-close style="flex:2;justify-content:center">{{ trans('collection.apply') }}</button>
    </div>
  </div>
</div>
