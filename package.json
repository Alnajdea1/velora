{
  "name": "lavender-reimagined-salla-theme",
  "version": "1.2.20",
  "description": "Lavender, Reimagined — beauty & fragrance Salla Twilight theme, Arabic RTL first",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build-theme.mjs",
    "production": "node scripts/build-theme.mjs",
    "development": "node scripts/build-theme.mjs",
    "dev": "salla theme preview --with-editor",
    "preview": "salla theme preview --with-editor",
    "check": "npm run production && node scripts/validate-theme.mjs",
    "watch": "node scripts/watch-theme.mjs",
    "publish": "salla theme publish"
  },
  "dependencies": {
    "@salla.sa/twilight": "^2.14.543",
    "@salla.sa/twilight-components": "^2.14.544"
  },
  "devDependencies": {
    "@salla.sa/cli": "^3.2.52",
    "copy-webpack-plugin": "^14.0.0",
    "webpack": "^5.109.2"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}
